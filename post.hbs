{{!< default}}
<style>
    .single-media img {
    width: 100%;
    max-height: 400px;
}
</style>
<div class="content-area">
    <main class="site-main"><div>s</div>
        {{#post}}
            {{> content width="narrow"}}
        {{/post}}
        {{#is "post"}}
            {{#if @custom.show_related_posts}}
                {{> related}}
            {{/if}}
            {{> comment}}
        {{/is}}
    </main>
</div>

{{#contentFor "body_class"}}{{#if @site.cover_image}} with-alt-header{{/if}}{{/contentFor}}